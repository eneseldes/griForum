/**
 * AuthContext
 * 
 * Authentication context object'i.
 * AuthProvider ve useAuth tarafından kullanılır.
 */

import { createContext } from "react";

export const AuthContext = createContext(null);

