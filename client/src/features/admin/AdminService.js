/**
 * Admin Service
 * 
 * Admin ile ilgili API çağrılarını yöneten service. Şu anda boş,
 * gelecekte admin işlevleri burada implement edilecek.
 */

import { api } from "../shared/api";

const withAuth = true;

export const AdminService = {
  // Add admin-specific API calls here
};

export default AdminService;

